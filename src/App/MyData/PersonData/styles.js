import { primaryColor, fontSizeSmall, fontWeightMuted } from '@ziro/theme'

export const

  container = {
    display: 'grid',
    gridRowGap: '5px',
    marginTop: '15px'
  },

  update = {
    margin: '25px 0',
    textAlign: 'left',
    fontSize: fontSizeSmall,
    fontWeight: fontWeightMuted,
    color: primaryColor
  }